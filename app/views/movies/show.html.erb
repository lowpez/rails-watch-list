<div class="position-relative">
  <div class="card-category card-large" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('movie_background.jpg') %>)">
    <h1><%= @movie.title %></h1>
    <p><%= @movie.overview %></p>
  </div>
    <span class="movie-tag">-</span>
</div>
<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
    <span class="mb-2 d-block"><small><em class="text-muted"><%= "You haven't saved any movies yet!" if @movie %></em></small></span>
    <!--<div>
      <%= link_to 'add a movie', new_movie_path(@movie), class: 'btn btn-flat' %>
      <%= link_to 'detete this movie', movie_path(@movie), method: :delete, class: 'btn btn-cancel', data: { confirm: 'Are you sure you want to delete this movie?' } %>
    </div>-->
  </div>
</div>